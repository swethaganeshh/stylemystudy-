# Maestro Agent Bundle Configuration
entrypoint: main.py
description: A Dockerized FastAPI application for style-selector-agent
version: 0.1.0
dockerfile: Dockerfile
name: style-selector-agent

# Dependencies needed by this agent
requirements:
  - fastapi>=0.104.0
  - uvicorn[standard]>=0.24.0

# Additional metadata
metadata:
  author: Your Name
  tags:
    - fastapi
    - docker
    - example
