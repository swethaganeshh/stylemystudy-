# Maestro Agent Bundle Configuration
entrypoint: main.py
description: A Dockerized FastAPI application with sample CRUD endpoints
version: 0.1.0
dockerfile: Dockerfile
name: test

# Dependencies needed by this agent
requirements:
  - fastapi>=0.104.0
  - uvicorn[standard]>=0.24.0

# Additional metadata
metadata:
  author: Your Name
  tags:
    - fastapi
    - docker
    - example
